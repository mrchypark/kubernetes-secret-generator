apiVersion: secretgenerator.mittwald.de/v1alpha1
kind: SSHKeyPair
metadata:
  name: example-sshkeypair
spec:
  algorithm: ed25519
  # Random generation is the default. For deterministic Ed25519 generation,
  # see docs/SSHKEYPAIR.md and replace this comment with an ed25519Seed field.
  # Never commit a real seed to source control.
